"""SHL Conversational Assessment Recommender — application package."""
from dotenv import load_dotenv

# Load local .env for development; in production (Render) real env vars take precedence.
load_dotenv()

